// "use client";
import "slick-carousel/slick/slick.css";
import "slick-carousel/slick/slick-theme.css";
import { useSelector } from "react-redux";
import { useEffect } from "react";
import {
  Account, AddressManager, MyOrders, OrderDetails, ContactUs, TermsAndConditions,
  Reports, Checkout, OrderSummary, PaymentStatus, CategoryDetails, DetailNavbar, Stores,
  StoreDetails, Brands, BrandDetails, Home, Navbar, Categories, MobileNavigation,
  SearchBoxSuggestion, SideNav, Favorite, ProductDetails, Cart,
  Login,
  SignUp,
  ChangePassword,
  ChangePhoneNumber,
  AddNewAddress,
  PageNotFound,
} from "@imports";

const home = () => {
  return (
    <div>home</div>
  )
}

export default home